import React from 'react';
import RouterListener from '@/components/router-listener';

const App: React.FC = () => {
	return (
		<div className="App">
			<RouterListener />
		</div>
	);
};

// 测试格式化：故意添加不规范的空格和缩进  
const   test2   =   "world"   ;   
let    x    =    1    ;

export default App;
