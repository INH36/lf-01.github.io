import React from 'react';
import RouterListener from '@/components/router-listener';

const App: React.FC = () => {
   
    return (
        <div className='App'>
            <RouterListener />
        </div>
    );
};

export default App;
